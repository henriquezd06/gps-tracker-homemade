# gps-tracker-homemade
This system captures the GPS location of an Android device and transmits it in real time to a web page deployed on AWS, where it is displayed on an interactive map, the vehicle’s route is traced, and the UDP packets are recorded in a network sniffer. Infrastructure: EC2, RDS PostgreSQL, Nginx, Node.js and WebSocket.
